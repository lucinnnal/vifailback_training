# Host driver here only supports up to CUDA 12.8, so torch must come from
# the cu128 wheel index (the default PyPI torch build may not detect the GPU).
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
torchvision==0.26.0+cu128
torchaudio==2.11.0+cu128

transformers==5.7.0
trl==1.9.2
peft==0.20.0
deepspeed==0.19.5
accelerate==1.14.0
pyyaml
pillow
tensorboard

# Already installed in the vifailback_train conda env:
#   torch==2.11.0+cu128, torchvision, torchaudio (driver only supports up to CUDA 12.8)
#   transformers==5.7.0, trl==1.9.2, peft==0.20.0, deepspeed==0.19.5, accelerate==1.14.0
# Listed here for reference / reproducing the env elsewhere.
trl>=0.24.0
peft>=0.18.0
deepspeed>=0.18.0
accelerate>=1.11.0
pyyaml
pillow
tensorboard
